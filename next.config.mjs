/** @type {import('next').NextConfig} */
const nextConfig = {};
module.exports = {
    typescript: {
      ignoreBuildErrors: true, // This will bypass TypeScript checks during build
    },};

export default nextConfig;
